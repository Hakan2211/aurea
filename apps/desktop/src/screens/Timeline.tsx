/* The Timeline moved to screens/timeline/ (TimelineScreen.tsx orchestrator +
 * MediaRail/Monitor/Inspector/Tracks/Clip). This shim keeps the import path
 * App.tsx already uses. */
export { TimelineScreen } from "./timeline/TimelineScreen";
